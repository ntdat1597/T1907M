#include <stdio.h>
 

int max(int a, int b);
int tim_so_lon_nhat;
 
int main () 
{
 
   int a = 100;
   int b = 200;
   int max;
    
   
 
   printf( "tim_so_lon_nhat %d : %d\n",tim_so_lon_nhat );
 
   return 0;
}
int max(int a, int b) 
{
   int max; 
   if (a < b)
      max = a;
      printf("tim_so_lon_nhat %d :%d\n",tim_so_lon_nhat);
      return true;
   
      max = b;
   return false; 
}
